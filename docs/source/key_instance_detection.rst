Key instance detection
========================

One of the practical advantages of the MIL framing is that the model can
point back to *which instance* (e.g. which conformer) most influenced a
given prediction — the "key instance."

.. code-block:: python

   TODO: minimal example extracting key instances for a prediction

TODO: explain the mechanism used (e.g. attention weights, per-instance
scores) and how to interpret/visualize the output.

.. note::

   This page should include a worked example on a real dataset — this was
   flagged as advertised-but-not-demonstrated in earlier manuscript review,
   so make sure the docs example is concrete and runnable, not just
   conceptual.
