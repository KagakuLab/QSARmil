Consensus modelling
=====================

``MultiConformerEstimator`` (and its ``MultiConformerRegressor`` /
``MultiConformerClassifier`` subclasses) can combine predictions across
multiple underlying models into a consensus prediction, via the companion
package `QSARcons <https://github.com/KagakuLab/QSARcons>`_.

.. code-block:: python

   TODO: minimal example building a consensus model

TODO: document which combination strategies are available (e.g. averaging,
weighted averaging, stacking) and how to configure them.

.. seealso::

   :doc:`ecosystem` for how QSARcons relates to QSARmil and milearn.
