Hyperparameter optimization
==============================

The built-in hyperparameter search (``hopt``), its default search space,
and the ``LazyMIL`` convenience interface.

*Placeholder — content to be added.*
